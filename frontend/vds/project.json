{
  "name": "vds",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "vds/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/vds",
        "command": "node vds/build.js --projectRoot={projectRoot}"
      }
    }
  }
}
